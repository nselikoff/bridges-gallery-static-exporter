{
  "name": "bridges-gallery-static-exporter",
  "version": "1.0.0",
  "description": "Transform XML catalog export into static HTML",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf build/exhibitions && rm -rf build/css && node build.js && cp -r public/css build/css",
    "export": "rm -rf export && node export.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "8.57.0",
    "eslint-config-prettier": "9.1.0",
    "handlebars": "4.7.8",
    "jsdom": "24.0.0",
    "prettier": "3.2.5",
    "xml2js": "0.6.2"
  }
}
